/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // server actions / route handlers handle our mutations
  },
};

export default nextConfig;
